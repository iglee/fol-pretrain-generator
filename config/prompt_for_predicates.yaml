model: gpt-4o
prompt: "Generate 20 predicates of the form Rainy(x), Walks(x), SendPackages(x), Intersect(x,y), and Loves(alex) about the topic {topic}. Do not copy them exactly as they are examples! (Also, almost all of them should be variables x. But also, try to use diverse names for a couple examples.) Format the data in the json form:\n{{topic: [TOPIC], predicates: [GENERATED PREDICATES]}} \n Simply state the data and nothing else. "
max_tokens: 2000
temperature: 1.0
top_p: 1.0
frequency_penalty: 0.0
presence_penalty: 0.0
